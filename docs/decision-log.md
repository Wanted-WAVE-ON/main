# 결정 로그

2026-09-01 항목은 최초 기획 논의에서 확정된 것이며, 그 논의가 SPEC·brief·ERD·아키텍처 문서의 출처입니다.
이후 항목은 구현하면서 원안을 바꾼 지점입니다.

| 날짜 | 결정 | 이유 | 대안 | 영향 |
|---|---|---|---|---|
| 2026-09-01 | 핵심을 Gesture Recognition이 아닌 Personalized Spatial Agent로 정의 | 사용자 제공 기획의 중심 메시지 유지 | 고정 명령 매핑 | 모든 DB·API·발표가 학습 루프 중심으로 정렬 |
| 2026-09-01 | 스키마 축을 `gesture → command` 매핑이 아닌 `Observation → Pattern → Memory → Feedback`으로 고정 | 학습 루프가 테이블에 남아야 나중에 LLM이나 다른 Agent 모델을 붙여도 구조를 갈아엎지 않음 | gesture-command 매핑 테이블 | 확장 시 Action Executor만 추가하면 되고 기억·피드백 구조는 유지됨 |
| 2026-09-01 | MVP를 Presentation·Music 두 맥락으로 자르고 주방·IoT·공간 자동 인식·LLM을 전부 제외 | 이 범위를 동시에 구현하면 완주 자체가 위험 | 확장 시나리오까지 한 번에 구현 | 데모가 학습 루프 한 가지에 집중됨. 제외 항목은 확장 시나리오로 설명(PLAN 2장 P2) |
| 2026-09-01 | 공간(`space`)은 컬럼으로만 두고 자동 인식하지 않음 | MVP에서 공간 추론까지 욕심내면 정확도 책임이 커짐 | 공간 자동 감지 | `space`는 관찰 시점 스냅샷 값으로만 기록되고 학습 단위는 `activity` |
| 2026-09-01 | 로컬 SQLite 채택 | 해커톤 설치와 재현성 | PostgreSQL | 단일 PC 데모에 최적, 운영 확장 시 DB 교체 필요 |
| 2026-09-01 | FastAPI + Vanilla JS 단일 서버 | 실행 명령 단순화 | Streamlit, React | 프론트와 API를 한 번에 실행 가능 |
| 2026-09-01 | 3회 반복 시 제안 | 제공 기획의 MVP 데모 흐름 반영 | 5회 이상, 동적 threshold | 빠른 학습 시연 가능, 운영에서는 조정 필요 |
| 2026-09-01 | 승인 전 자동 실행 금지 | 오작동 방지와 사용자 통제 | 즉시 자동 등록 | Agent 제안 UX가 핵심 차별점이 됨 |
| 2026-09-01 | `executions` 테이블 추가 | 관계도에는 있으나 상세 정의가 없어 감사 로그 보강 필요 | Action 테이블 재사용 | 실행 결과와 Feedback을 정확히 연결 |
| 2026-09-01 | 기본 Action Executor를 DRY_RUN으로 설정 | 발표 현장의 예기치 않은 키 입력 방지 | 항상 OS 제어 | 안전하고 재현 가능한 데모 |
| 2026-09-01 | 웹캠은 선택 기능, 버튼 시뮬레이션은 기본 | 하드웨어·조명 위험 관리 | 웹캠만 제공 | 심사 시 안정성과 실제성 모두 확보 |
| 2026-09-02 | 원안의 6개 패키지(`perception/`, `context/`, `agent/`, `actions/`, `api/`, `db/`) 대신 `routers/` + `services/` 2계층 채택 | 모듈 대부분이 파일 1개짜리라 껍데기만 늘어남 | 원안대로 6개 패키지 | 카메라·IoT 어댑터가 실제로 늘어나면 그때 분리 |
| 2026-09-02 | 몸짓 직후 행동을 "3초 이내 자동 상관"이 아니라 `POST /teach`의 `observation_id`로 명시 연결 | 시간창 자동 연결은 무관한 입력을 잘못 묶고, 그 오연결이 그대로 학습 데이터가 됨 | 3초 시간창 자동 상관 | 관찰 1건에 행동 1건이 보장됨(L-1). 대신 사용자가 후속 행동을 직접 선택해야 함 |
| 2026-09-04 | 웹캠 감지를 ROI 중앙값에서 움직이는 픽셀의 Optical Flow 평균으로 교체 | ROI 중앙값이 배경 노이즈에 묻혀 swipe를 놓침 | ROI 튜닝 유지 | 조명·배경 편차에 덜 민감해졌으나 하드웨어 검증은 여전히 필요(FR-17) |
| 2026-09-04 | 규범적 규칙을 `SPEC.md` 한 곳으로 모으고 문서 충돌 시 우선권 부여 | 규칙이 README·docs·코드에 흩어져 서로 어긋남 | 문서별 규칙 유지 | FR ↔ 규칙 ↔ 코드 ↔ 테스트 추적 가능 |
| 2026-09-05 | 워크벤치 UI로 재설계하고 디자인 시스템을 `docs/design.md`·`static/tokens.css`에 고정 | 화면의 주어를 "명령 설정"이 아닌 "AI가 배운 것"으로 바꾸기 위함(U-2) | 기존 3열 레이아웃 유지 | 페이지별 임의 테마 금지, 토큰만 사용 |
| 2026-09-05 | 실제 OS 키 입력 전 활성 창 검증을 기본 활성화(I-6) | 대상 앱이 아닌 창에 키가 들어가면 되돌릴 수 없음 | 항상 키 전송 | 확인 불가 환경에서는 전부 `FAILED`로 기록. 리눅스는 검증을 꺼야 함 |
| 2026-09-05 | `POST /demo/reset`을 단일 트랜잭션으로 처리(D-1) | 중간 실패 시 demo-user가 사라진 채로 남아 데모가 깨짐 | 삭제 후 재생성 2단계 | 실패해도 이전 상태로 롤백 |
